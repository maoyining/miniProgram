<!--index.wxml-->
<import src='/pages/template/list_sort_cmp.wxml'/>
<view class="container1">

    <view id='two'>
        <block wx:for="{{information}}" wx:key="{{index}}">
        <view catchtap="toTeamList"  data-index='{{index}}'>
        <template is="msgItem_sort" data="{{...item}}"/>
        </view>
        </block>
    </view>

</view>
